Install python 3.11 
Type the following command to install all dependencies pip install -r requirements.txt

Change mongodb url in db.py inside database folder

Change the port in main.py ### uvicorn.run(app, host="0.0.0.0", port=8000) ### to run your app on any other port

type python3 main.py to run your dashboard

